# ============================================================
# 水文地质科研环境 - 领域增强依赖（重依赖，按需安装，非必需）
#
# 本文件里的包体积大、依赖多（部分需编译原生扩展），
# CI / 沙箱建议不要默认全装，避免超时或装不上拖垮核心流程。
# 本地做大规模建模 / 生态服务估值时再装：
#   pip install -r requirements-domain.txt
# 或用 conda 路线（见 environment.yml 注释）。
# ============================================================

# ---------- 大规模 MODFLOW 建模 ----------
imod>=1.1                # Deltares：用 xarray 搭超大 MODFLOW 模型
# ---------- 从数据自动搭建水文模型 ----------
hydromt>=0.6             # Deltares：自动把 GIS/气象数据编译成模型输入
# ---------- 生态系统服务与环境经济估值 ----------
natcap.invest>=3.14      # Natural Capital Project：水源涵养/固碳/生境/休闲等货币化
# ---------- 分布式水文模型 ----------
wflow>=0.8                # Deltares：PCRaster 系的流域水文模拟
# ---------- 含水层补给量空间估算 ----------
pyRecharge>=0.1          # 空间分布式补给/排泄估算
# ---------- 数据分析辅助 ----------
whitebox>=2.3            # 已可在 pip 直装，沙箱可选
