# ============================================================
# 水文地质学科研环境 - pip 依赖清单（领域加强版）
# 适用：Linux 沙箱 / CI / 已有 GDAL 预编译轮子的环境
# 本地推荐改用 environment.yml（conda-forge 的 GDAL 最稳）
#
# 安装：python -m pip install --upgrade pip && pip install -r requirements.txt
# 校验：python scripts/test_check.py
#
# 本版新增（针对 水土评价 + 环境经济评价）：
#   pastas / gstools / pykrige / pyemu / spotpy / pyet /
#   scikit-criteria / numpy-financial / hydrofunctions / dataretrieval /
#   cartopy / xskillscore / phydrus / timml / leafmap
# 重依赖（imod / hydromt / natcap.invest 等）见 requirements-domain.txt，可选装。
# ============================================================

# ---------- 科学计算底座 ----------
numpy>=1.26,<3
pandas>=2.2
scipy>=1.13
xarray>=2024.6

# ---------- 栅格 / 遥感 ----------
rasterio>=1.3.10
rioxarray>=0.15
# GDAL 的 Python 绑定：Linux 下需系统 libgdal-dev；conda 环境请改用 environment.yml
# GDAL>=3.6
rasterstats>=0.19
earthengine-api>=0.1.400
geemap>=0.34.0
leafmap>=0.43            # 交互式地图（geemap 的轻量替代，无需 GEE 账号）

# ---------- 矢量 / 坐标 ----------
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
fiona>=1.9

# ---------- 气象水文格点数据 ----------
netCDF4>=1.6.5
h5py>=3.11

# ---------- 地下水数值模拟 ----------
flopy>=3.6.0            # MODFLOW-6 / MODFLOW-2005 / MT3DMS Python 驱动

# ---------- 水文地形分析 ----------
pysheds>=0.3.5          # 洼地填平 / 流向 / 汇流累积 / 流域提取
richdem>=0.3.4          # 高性能地形分析（可选，Windows 需编译）
whitebox>=2.3            # 白盒工具集（填洼、流向、水文校正）

# ---------- 水文模型评价指标 ----------
hydroeval>=0.0.1        # NSE / KGE / PBIAS / VE
pymannkendall>=1.4.3    # MK 趋势检验 / Sen 斜率
statsmodels>=0.14       # 时序统计、STL 分解、回归
scikit-learn>=1.5       # 聚类/分类/降维（分区、异常检测）

# ============================================================
# 领域增强：水土评价 + 环境经济评价（核心新增）
# ============================================================

# ---------- 地下水头响应时序建模 ----------
pastas>=1.14             # 含水层对降水/抽水的响应、补给估计、噪声模型

# ---------- 空间统计 / 克里金插值 ----------
gstools>=1.6             # 地统计：变异函数拟合、随机场、克里金
pykrige>=1.7             # 普通/泛克里金（head/水位 空间插值）

# ---------- 参数率定与不确定性 ----------
pyemu>=1.5               # PEST(++) 接口、灵敏度、数据价值分析
spotpy>=1.6              # GLUE / DREAM / 多种参数优化与不确定性

# ---------- 蒸散发（水量平衡关键项） ----------
pyet                     # FAO-56 Penman-Monteith 参考蒸散发与潜在蒸散

# ---------- 多准则决策分析（土地适宜性 / 脆弱性分区） ----------
scikit-criteria>=0.8     # AHP / TOPSIS / WPM 等 MCDA（水土评价核心）

# ---------- 环境经济评价（成本效益分析） ----------
numpy-financial>=1.0     # NPV / IRR / 现值 / 年金

# ---------- 自动拉取观测数据 ----------
hydrofunctions          # USGS NWIS 取径流/水位
dataretrieval           # USGS 多类型水文数据拉取（含 NWIS、水化学）

# ---------- 制图与预报检验 ----------
cartopy>=0.23            # 地图投影、底图（论文级）
xskillscore>=0.0.26      # 网格/预报技能评分（KGE、MSESS 等）
matplotlib>=3.8
seaborn>=0.13
contextily>=1.6         # 在线底图
folium>=0.16

# ---------- 包气带 / 解析法模型 ----------
phydrus>=0.1             # HYDRUS-1D 包气带水流与溶质
timml>=6.9               # 多层解析元模型（TimML/TTim 同一包）

# ---------- 工程化 ----------
pyyaml>=6.0
loguru>=0.7
tqdm>=4.66
pytest>=8.2
openpyxl>=3.1           # 观测数据 xlsx 读写
